# 🤖 ربات تلگرام هوش مصنوعی با OpenRouter

یک ربات تلگرام قدرتمند و چندمنظوره که از طریق سرویس OpenRouter به مدل‌های مختلف هوش مصنوعی متصل می‌شود. این ربات به شما امکان می‌دهد با استفاده از مدل‌های پیشرفته مانند DeepSeek و Gemma به گفتگو بپردازید.

## ✨ ویژگی‌ها

- 🎯 **پشتیبانی از چند مدل**: استفاده از مدل‌های مختلف هوش مصنوعی (DeepSeek و Gemma)
- 💬 **تاریخچه گفتگو**: حفظ تاریخچه مکالمات تا 10 پیام اخیر برای هر کاربر
- 🎨 **منوی تعاملی**: رابط کاربری زیبا با دکمه‌های اینلاین
- 🔐 **امنیت**: استفاده از فایل `.env` برای نگهداری کلیدهای API
- 🌐 **چندزبانه**: پشتیبانی کامل از زبان فارسی
- 📊 **لاگ‌گیری**: سیستم لاگ کامل برای رفع خطاها
- ⚡ **سریع و کارآمد**: مدیریت همزمان درخواست‌های چند کاربر

## 🚀 پیش‌نیازها

- Python 3.8 یا بالاتر
- حساب OpenRouter (برای دریافت کلید API)
- ربات تلگرام (از [@BotFather](https://t.me/BotFather))

## 📦 نصب

1. **کلون کردن مخزن:**
   ```bash
   git clone https://github.com/yourusername/Ai-chat-bot.git
   cd Ai-chat-bot
   ```

2. **نصب وابستگی‌ها:**
   ```bash
   pip install -r requirements.txt
   ```

3. **ایجاد فایل `.env`:**
   در ریشه پروژه فایل `.env` را ایجاد کرده و متغیرهای زیر را اضافه کنید:
   ```env
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token
   OPENROUTER_API_KEY_DEEPSEEK=your_deepseek_api_key
   OPENROUTER_API_KEY_GEMMA=your_gemma_api_key
   YOUR_SITE_URL=http://t.me/your_bot_username
   YOUR_APP_NAME=Telegram LLM Bot
   ```

## ⚙️ پیکربندی

### دریافت توکن تلگرام

1. به [@BotFather](https://t.me/BotFather) در تلگرام مراجعه کنید
2. دستور `/newbot` را ارسال کنید
3. نام و یوزرنیم ربات را انتخاب کنید
4. توکن دریافتی را در فایل `.env` قرار دهید

### دریافت کلید API از OpenRouter

1. به [OpenRouter](https://openrouter.ai/) مراجعه کنید
2. حساب کاربری ایجاد کنید یا وارد شوید
3. از بخش API Keys کلید API خود را دریافت کنید
4. کلیدها را در فایل `.env` قرار دهید

**نکته:** کلید API OpenRouter باید با `sk-or-` شروع شود.

## 🎮 نحوه استفاده

1. **اجرای ربات:**
   ```bash
   python bot.py
   ```

2. **در تلگرام:**
   - ربات را پیدا کنید و دستور `/start` را ارسال کنید
   - از منوی اصلی یک مدل هوش مصنوعی را انتخاب کنید
   - شروع به چت کردن کنید!

## 📋 دستورات

- `/start` - شروع ربات و نمایش منوی اصلی
- `/menu` - نمایش منوی اصلی

## 🏗️ ساختار پروژه

```
Ai-chat-bot/
│
├── bot.py              # فایل اصلی ربات
├── requirements.txt    # وابستگی‌های پایتون
├── .env               # فایل تنظیمات (ایجاد کنید)
└── README.md          # این فایل
```

## 🔧 توابع اصلی

- `call_openrouter_api()` - ارسال درخواست به API OpenRouter
- `show_main_menu()` - نمایش منوی اصلی
- `handle_message()` - پردازش پیام‌های کاربر
- `button_callback()` - مدیریت کلیک روی دکمه‌ها

## 🛡️ مدیریت خطا

ربات شامل سیستم مدیریت خطای جامعی است که خطاهای زیر را مدیریت می‌کند:

- خطاهای اتصال به API
- خطاهای احراز هویت
- خطاهای timeout
- خطاهای فرمت پاسخ

## 📝 نکات مهم

- تاریخچه گفتگو برای هر کاربر به صورت جداگانه نگهداری می‌شود
- با تغییر مدل، تاریخچه قبلی پاک می‌شود
- حداکثر 10 پیام در تاریخچه نگهداری می‌شود
- در صورت عدم انتخاب مدل، ربات به کاربر یادآوری می‌کند

## 🤝 مشارکت

مشارکت‌ها همیشه خوشآمد هستند! لطفاً:

1. Fork کنید
2. یک شاخه جدید ایجاد کنید (`git checkout -b feature/AmazingFeature`)
3. تغییرات خود را commit کنید (`git commit -m 'Add some AmazingFeature'`)
4. به شاخه push کنید (`git push origin feature/AmazingFeature`)
5. یک Pull Request باز کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 👤 نویسنده

ساخته شده با ❤️ توسط شما

## 🙏 تشکر

- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot)
- [OpenRouter](https://openrouter.ai/)
- [DeepSeek](https://www.deepseek.com/)
- [Google Gemma](https://ai.google.dev/gemma)

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال، لطفاً یک Issue ایجاد کنید.

---

⭐ اگر این پروژه برایتان مفید بود، به آن ستاره بدهید!

